"""Security guardrails for ArenaOS input validation.

Provides deterministic threat pattern matching to screen user inputs
against known attack vectors including prompt injection, XSS, path
traversal, SQL injection, and command injection.
"""
